{
  "name": "mountfd-site",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i styles.css -o dist/styles.css --minify && cp index.html favicon.svg dist/"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
